shape: [32, 32]

run:
  fps: 30
  seconds: 3

animation:
    typename: sequence
    length: 1
    animations:
        - $bpa.matrix.ImageAnim
        - $bpa.matrix.ImageShow
        - $bpa.matrix.ImageDissolve
