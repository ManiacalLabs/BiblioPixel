controls:
  typename: .rest
  verbose: true

shape: [32, 32]
numbers: float
animation:
  typename: mixer
  levels: [1, 1, 1]
  animations:
    - {typename: .fill, color: red}
    - {typename: .fill, color: green}
    - {typename: .fill, color: blue}
